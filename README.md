# Denoising-Auto-Encoders
Denoising Autoencoder (DAE) built with TensorFlow/Keras to remove noise from images. Learns robust representations by reconstructing clean images from noisy inputs, useful for image restoration and preprocessing tasks.
